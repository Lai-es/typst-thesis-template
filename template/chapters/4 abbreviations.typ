#let abbreviations()=[
  #v(0.7em)
  #align(center)[
    //to convert from other file formats (Excel etc.):
    //https://www.latex-tables.com/?format=typst&force
    
    //could look something like this:
  #table(columns:2,
    table.header([Abbreviation],[Meaning]),
    [ABC],[Ammonium bicarbonate],
    [ACN],[Acetonitrile],
    [...], [...],
    
  )]
  
]